## Pay with Sand SDK - Example environment file
## Do NOT commit real secrets.

# --- Network selection ---
# Defaults to 137 (Polygon) if omitted
VITE_PAY_WITH_SAND_CHAIN_ID=137
# Alternatively, you can use this key (also read by the SDK):
VITE_CHAIN_ID=137

# --- Contracts per chain (preferred) ---
# Built-in defaults exist for Polygon (137), override if needed
VITE_PAYMENT_CONTRACT_ADDRESS_137=0xB15626D438168b4906c28716F0abEF3683287924
VITE_SAND_TOKEN_ADDRESS_137=0xBbba073C31bF03b8ACf7c28EF0738DeCF3695683
# Non‑Vite (plain env) per‑chain examples supported by the SDK as well:
PAYMENT_CONTRACT_ADDRESS_137=0xB15626D438168b4906c28716F0abEF3683287924
SAND_TOKEN_ADDRESS_137=0xBbba073C31bF03b8ACf7c28EF0738DeCF3695683

# Optional global fallbacks (discouraged; use per-chain vars above)
VITE_PAYMENT_CONTRACT_ADDRESS=
REACT_APP_PAYMENT_CONTRACT_ADDRESS=
# Global SAND token fallback keys (used if per-chain is not provided)
VITE_SAND_TOKEN_ADDRESS=
SAND_TOKEN_ADDRESS=

# --- WalletConnect / RPC (optional) ---
# Used by WalletConnect for Polygon RPC if applicable
VITE_INFURA_ID=

# Optional: override price API used to compute USD value in the UI.
# The endpoint can return:
#  - A plain number (text/plain or a root-level JSON number), e.g. 0.42
#  - JSON like: { "the-sandbox": { "usd": number } }
#  - Other common shapes (usd/price/priceUsd, arrays, market_data.current_price.usd...)
# You can also specify an explicit JSON path to the numeric price:
# PRICE_API_JSON_PATH=data.token.usd
# PRICE_API_URL=https://your-proxy.example.com/the-sandbox-price
