## Pay with Sand SDK - Example environment file
## Do NOT commit real secrets.

# On-chain payment contract address (legacy single-network fallback)
REACT_APP_PAYMENT_CONTRACT_ADDRESS=0xYourContractAddress

# Optional: override price API used to compute USD value in the UI.
# The endpoint can return:
#  - A plain number (text/plain or a root-level JSON number), e.g. 0.42
#  - JSON like: { "the-sandbox": { "usd": number } }
#  - Other common shapes (usd/price/priceUsd, arrays, market_data.current_price.usd...)
# You can also specify an explicit JSON path to the numeric price:
# PRICE_API_JSON_PATH=data.token.usd
# PRICE_API_URL=https://your-proxy.example.com/the-sandbox-price
